turple:元组
元组自从定义开始，就再也不能改变了
实际使用上十分相似的

创建元组的时候基本是用小括号
注意，创建的时候，逗号是关键
tuple2 = (1,) 创建了元组
tuple2 = 1,3,4,5 创建了元组
tuple2 = (1) 创建了整形
tuple2 = () 创建了空元组
如果只创建了一个元素的话，一定要注意加“，”以防被认为是整形

更新和删除元组：
更新：新建一个新的然后复制过去hhh
temp = temp[:2]+(1,)
实际上原来那个temp还在，不过是temp指针已经不指向他了
它新指向了一个新的地方，那个里边有这些新的东东

删除：del temp 全部删除
不过一般不这么明显的删。。。
python回收机制：静态的小数据如果以后没人指向他了以后，他就从内存里被弄丢了