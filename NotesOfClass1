数组和列表

数组：数据类型必须一致
可是python没有数据类型。。。

所以有列表，里边丢啥东西他都吃
甚至对象都可以（类）

列表里边还可以丢列表

列表里边丢东西,丢一个东西
用append
比如 member.append("福鹿娃娃")
len(member)长度

丢几个东西，用extend
extend([111,222,333])
extend里边丢的是列表
方法等同于两个列表用加号相连
Ps：加法两边同一个类型
列表加列表

insert,往中间加
insert(1,‘牡丹’)
1的意思是，我加进去以后位置就是1了
即第二个是牡丹

remove删除，括号里加他是谁
del 语句,del member[1]
第二个就没了

pop() 弹栈
member.pop()
换句话说最后那个就弹出来了(所以他就没了！！！)
顺便返回他是谁
pop(1) 索引为1的被弹出来，木得了

slice
列表切片member[1:3]
把索引之中编号为1到2的全拷贝出来
如果冒号前不写，默认是第一个开始
冒号后不写，默认到最后一个
前后都不写，拷贝整个列表
注意，这个是拷贝
如果直接写a=b，相当于只是弄了一个指针过去
b变了a也会变！
所以写a=b[:]

列表比大小一般就是比ASCII码

list可以直接乘法

元素操作：
a in list3 会返回 ture 或者 false
注意，只会刨一层
多层访问：多个[][][][][][]

dir:列举
dir(list)
把列表的所有功能显示出来
显示的是内部的方法，所以是.
比如a.count(3),数有几个3
a.index(123)，返回第一个出现的索引

比较重要的几个方法
reverse翻转整个列表

sort() 用指定的方法从小到大进行排队
三个参数
sort(func,key,reverse)
func:默认为归并排序
key:不管
reverse:默认false，改为true就从大到小排